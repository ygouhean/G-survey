# ============================================
# G-SURVEY - Configuration d'Environnement
# ============================================

# --------------------------------------------
# Server Configuration
# --------------------------------------------
# Port du serveur backend
PORT=5000

# Environnement (development, production, test)
NODE_ENV=development

# --------------------------------------------
# MongoDB Configuration
# --------------------------------------------
# URI de connexion MongoDB
# Format: mongodb://[username:password@]host[:port]/database
# Exemples:
#   - Local: mongodb://localhost:27017/gsurvey
#   - Docker: mongodb://host.docker.internal:27017/gsurvey
#   - MongoDB Atlas: mongodb+srv://user:pass@cluster.mongodb.net/gsurvey
MONGODB_URI=mongodb://localhost:27017/gsurvey

# --------------------------------------------
# JWT Configuration
# --------------------------------------------
# Clé secrète pour signer les tokens JWT
# ⚠️ IMPORTANT: Changez cette valeur en production !
# Générez une clé sécurisée avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=g-survey-super-secret-jwt-key-2024-change-me-in-production

# Durée de validité des tokens (7d = 7 jours)
# Formats acceptés: 60, "2 days", "10h", "7d"
JWT_EXPIRE=7d

# --------------------------------------------
# Default Admin User
# --------------------------------------------
# Email de l'administrateur par défaut
ADMIN_EMAIL=admin@gsurvey.com

# Mot de passe de l'administrateur par défaut
# ⚠️ IMPORTANT: Changez ce mot de passe dès la première connexion !
ADMIN_PASSWORD=Admin@123

# --------------------------------------------
# CORS Configuration
# --------------------------------------------
# URL du client frontend (pour autoriser les requêtes CORS)
# En production, utilisez votre domaine: https://votre-domaine.com
CLIENT_URL=http://localhost:5173

# --------------------------------------------
# Map Configuration (Optionnel)
# --------------------------------------------
# Token Mapbox pour des cartes avancées (optionnel)
# Obtenez un token gratuit sur: https://www.mapbox.com/
MAPBOX_TOKEN=your_mapbox_token_here

# Centre de carte par défaut (Paris, France)
DEFAULT_MAP_CENTER_LAT=48.8566
DEFAULT_MAP_CENTER_LNG=2.3522
DEFAULT_MAP_ZOOM=6

# --------------------------------------------
# Email Configuration (Optionnel - Future)
# --------------------------------------------
# Configuration SMTP pour l'envoi d'emails
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# EMAIL_FROM=noreply@gsurvey.com

# --------------------------------------------
# Upload Configuration (Optionnel - Future)
# --------------------------------------------
# Taille maximale des fichiers uploadés (en bytes)
# MAX_FILE_SIZE=5242880  # 5MB

# Dossier de stockage des uploads
# UPLOAD_DIR=./uploads

# --------------------------------------------
# Logging Configuration (Optionnel - Future)
# --------------------------------------------
# Niveau de logging (error, warn, info, debug)
# LOG_LEVEL=info

# Dossier des logs
# LOG_DIR=./logs

# --------------------------------------------
# Redis Configuration (Optionnel - Future)
# --------------------------------------------
# Pour la mise en cache et les sessions
# REDIS_URL=redis://localhost:6379

# --------------------------------------------
# Analytics Configuration (Optionnel - Future)
# --------------------------------------------
# Google Analytics
# GA_TRACKING_ID=UA-XXXXXXXXX-X

# --------------------------------------------
# Feature Flags (Optionnel - Future)
# --------------------------------------------
# Activer/désactiver des fonctionnalités
# ENABLE_OFFLINE_MODE=true
# ENABLE_EMAIL_NOTIFICATIONS=false
# ENABLE_SMS_NOTIFICATIONS=false

# --------------------------------------------
# Notes de Sécurité
# --------------------------------------------
# ⚠️ Ne commitez JAMAIS ce fichier avec des vraies valeurs !
# ⚠️ Utilisez .env.example comme template
# ⚠️ Créez un .env local avec vos vraies valeurs
# ⚠️ Assurez-vous que .env est dans .gitignore
